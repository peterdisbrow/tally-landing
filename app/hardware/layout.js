export const metadata = {
  title: 'Hardware & Software Compatibility | Tally',
  description: 'Check if your church production gear works with Tally. Compatible with ATEM switchers, OBS, vMix, PTZ cameras, HyperDeck, Companion, and more.',
};

export default function HardwareLayout({ children }) {
  return children;
}
