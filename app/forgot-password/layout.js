export const metadata = {
  title: 'Forgot Password — Tally',
  description: 'Reset your Tally account password. Enter your email to receive a password reset link.',
  robots: { index: false, follow: false },
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
