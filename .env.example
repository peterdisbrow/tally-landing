# Tally Landing Page — Environment Variables
# Copy this to .env.local and fill in the values.

# ─── Required ─────────────────────────────────────────────────────────────────

# Relay server URL (your Railway deployment)
RELAY_URL=https://your-relay.up.railway.app
NEXT_PUBLIC_RELAY_URL=https://your-relay.up.railway.app

# Session secret for password reset tokens (min 32 chars)
SESSION_SECRET=change-me-to-a-random-string

# Admin key to authenticate with the relay server
RELAY_ADMIN_KEY=your-admin-api-key

# ─── Email (Resend) ──────────────────────────────────────────────────────────

RESEND_API_KEY=re_xxxxxxxxxxxx
FROM_EMAIL=Tally <noreply@atemschool.com>

# ─── Rate Limiting (Upstash Redis) ───────────────────────────────────────────

UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# ─── Optional ─────────────────────────────────────────────────────────────────

# Public app URL (used for email links, OG images)
NEXT_PUBLIC_APP_URL=https://tallyconnect.app

# Plausible analytics domain
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=tallyconnect.app

# Mailchimp (early access waitlist)
MAILCHIMP_API_KEY=your-mailchimp-key
